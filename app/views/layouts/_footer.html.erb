<footer class="footer">

  <div class="container">
    <div class="row my-5">
      <div class="col">
        <p class="text-muted">Place sticky footer content here.</p>
        <% unless admin_signed_in? %>
          <div><%= link_to "利用規約", class: "text-muted" %></div>
        <% else %>
          <div><%= link_to "利用規約", class: "text-muted" %></div>
        <% end %>
      </div>
      <div>
        google map
        contact
      </div>
    </div>
  </div>
</footer>