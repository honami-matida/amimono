<h1>Work index</h1>

<% @works.each do |work| %>

  <p>title</p>
  <span>
    <%= link_to work.title, work_path(work.id) %>
  </span>

  <p>explanation</p>
  <span><%= work.body %></span>

  <p>image</p>
  <%= image_tag work.image, size: "200x200" %>

<% end %>